# Example reference

This is a placeholder reference file. The agent will read this via `read_skill_file("example", "references/notes.md")` to confirm stage-3 file loading works.

In a real skill, this directory holds detailed documentation that's too long to live in `SKILL.md` itself — domain-specific reference data, schemas, common edge cases, etc.
